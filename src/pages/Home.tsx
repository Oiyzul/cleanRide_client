import { Branding, Featured, Footer, Review } from "@/components";
import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Branding />
      <Featured />
    </div>
  );
};

export default Home;
